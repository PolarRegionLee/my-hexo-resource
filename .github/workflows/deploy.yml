name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout source repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        submodules: true
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '24'
        cache: 'yarn'
    
    - name: Install dependencies
      run: yarn install --frozen-lockfile
    
    - name: Generate static files
      run: yarn build
    
    - name: Deploy to polarregionlee.github.io
      uses: peaceiris/actions-gh-pages@v3
      with:
        personal_token: ${{ secrets.DEPLOY_TOKEN }}
        publish_dir: ./public
        external_repository: polarregionlee/polarregionlee.github.io
        publish_branch: main
        user_name: 'github-actions[bot]'
        user_email: 'github-actions[bot]@users.noreply.github.com'
        commit_message: 'Deploy to github.io from ${{ github.repository }}@${{ github.sha }}'
    
    - name: Prepare for blog.polarregionlee.com deployment
      run: |
        echo "blog.polarregionlee.com" > public/CNAME
    
    - name: Deploy to blog.polarregionlee.com
      uses: peaceiris/actions-gh-pages@v3
      with:
        personal_token: ${{ secrets.DEPLOY_TOKEN }}
        publish_dir: ./public
        external_repository: polarregionlee/blog
        publish_branch: main
        user_name: 'github-actions[bot]'
        user_email: 'github-actions[bot]@users.noreply.github.com'
        commit_message: 'Deploy to blog.polarregionlee.com from ${{ github.repository }}@${{ github.sha }}'